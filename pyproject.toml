[project]
name = "gglp-reviewscraper"
version = "0.1.0"
description = ""
authors = [
    {name = "dsalazarCazanhas",email = "dsalazarcazanhas@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14,<4.0"
dependencies = [
    "google-play-scraper (>=1.2.7,<2.0.0)",
    "pandas (>=3.0.2,<4.0.0)",
    "requests (>=2.33.1,<3.0.0)",
    "httpx (>=0.28.1,<0.29.0)"
]

[tool.poetry]
packages = [{include = "gglp_reviewscraper", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
